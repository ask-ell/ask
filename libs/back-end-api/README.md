# back-end-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build back-end-api` to build the library.

## Running unit tests

Run `nx test back-end-api` to execute the unit tests via [Jest](https://jestjs.io).
